import React, {useEffect, useState} from "react";
import { connect } from 'react-redux';
import FileUpload from "../FileUpload";

const UploadTest = (props) => {

        return (

    <FileUpload />

        )
            }


export default connect()(UploadTest);